name: Property Tracking & Analytics

on:
  schedule:
    # Run on the 2nd day of every month at 02:00 UTC (after scraping completes)
    - cron: '0 2 2 * *'
  workflow_dispatch:
  push:
    branches: ["main"]
    paths:
      - 'data/**/*.md'
      - 'scripts/track_properties.py'
      - 'scripts/visualize_tracking.py'
      - 'scripts/generate_tracking_index.py'

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: "tracking"
  cancel-in-progress: false

jobs:
  track-and-analyze:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Fetch all history for complete tracking
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
      
      - name: Run property tracking (all cities)
        run: |
          echo "🔍 Running property tracking for all cities..."
          python3 scripts/track_properties.py
      
      - name: Generate visualizations (all cities)
        run: |
          echo "📊 Generating visualizations for all cities..."
          python3 scripts/visualize_tracking.py
      
      - name: Generate tracking index pages
        run: |
          echo "📄 Generating tracking index and city pages..."
          python3 scripts/generate_tracking_index.py
      
      - name: Run MLIT API matching (if API key available)
        env:
          REINFOLIB_API_KEY: ${{ secrets.REINFOLIB_API_KEY }}
        run: |
          if [ -n "$REINFOLIB_API_KEY" ]; then
            echo "🏢 Running MLIT API matching..."
            python3 scripts/match_with_mlit.py
          else
            echo "⚠️  REINFOLIB_API_KEY not set, skipping MLIT matching"
          fi
        continue-on-error: true
      
      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
      
      - name: Commit tracking data
        run: |
          git add data/tracking/
          git add data/reports/
          git add docs/images/
          git add tracking.md
          git add tracking/
          git add data/mlit_matching_report.md || true
          
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "🤖 Update property tracking data [$(date +'%Y-%m')]"
            git push
          fi
      
      - name: Display tracking summary
        run: |
          echo "📈 Cities tracked:"
          ls data/tracking/ 2>/dev/null | grep tracking_db || echo "(none yet)"
          echo "📅 Monthly reports:"
          ls data/reports/ 2>/dev/null | grep -v latest || echo "(none yet)"

  build-and-deploy-pages:
    needs: track-and-analyze
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: main  # Ensure we get the latest changes
      
      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.2'
          bundler-cache: true
      
      - name: Setup Pages
        id: pages
        uses: actions/configure-pages@v5
      
      - name: Build with Jekyll
        run: bundle exec jekyll build --baseurl "${{ steps.pages.outputs.base_path }}"
        env:
          JEKYLL_ENV: production
      
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
      
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

